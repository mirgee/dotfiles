# [user]
#   name = Miroslav Kovar
#   email = miroslavkovar@protonmail.com
# 	signingkey = C7D5D13580936D88
[user]
	email = miroslav.kovar@absa.africa
	name = Miroslav Kovar
[color]
  ui = auto
[alias]
  c = commit
  s = status
  f = push --force-with-lease
  st = status
  aa = add --all
  au = add --update
  co = checkout
  lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %Cblue<%an>%Creset' --abbrev-commit --date=relative --all
  ll = log --oneline
  pf = !git remote | xargs -L1 git push --force-with-lease
  rb = rebase
  # https://stackoverflow.com/a/11688523/472927
  ss = "!f() { git stash show stash^{/$*} -p; }; f"
  sa = "!f() { git stash apply stash^{/$*}; }; f"
  sl = stash list
[core]
  excludesfile = ~/.gitignore_global
  autocrlf = input
  safecrlf = true
  editor = nvim
[pull]
  ff = only
[push]
  default = simple
[fetch]
  prune = true
[rebase]
  autosquash = true
[merge]
  conflictstyle = diff3
[rerere]
  enabled = true
[log]
  date = local
[diff]
  algorithm = patience
  compactionHeuristic = true
[credential]
  helper = store
[help]
  autocorrect = 1
[ui "color"]
  ui = always
[color]
  ui = true
[github]
  user = mirgee
[protocol]
  version = 2
[filter "lfs"]
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true
  clean = git-lfs clean -- %f
# [url "git@github.com:"]
# 	insteadOf = https://github.com/
# [gpg]
#     program = gpg2
# [interactive]
#     singleKey = true
# [commit]
# 	gpgsign = true
